############################################################
# === BACKEND SECRETS ===
############################################################

JWT_SECRET=placeholder
SEMS_ACCOUNT=placeholder
SEMS_PASSWORD=placeholder
SEMS_STATION_ID=placeholder


############################################################
# === DATABASE CONFIGURATION ===
############################################################

DB_NAME_PROD=homewatts_prod
DB_NAME_STAGING=homewatts_staging
DB_NAME_LOCAL=homewatts_local
DB_NAME_CACHE=homewatts_cache

# Shared DB credentials (same for all databases)
DB_USERNAME=placeholder
DB_PASSWORD=placeholder
DB_PORT=5432

# Connection strings injected into Spring Boot by docker-compose
SPRING_DATASOURCE_URL_PROD=jdbc:postgresql://db:${DB_PORT}/${DB_NAME_PROD}
SPRING_DATASOURCE_URL_STAGING=jdbc:postgresql://db:${DB_PORT}/${DB_NAME_STAGING}

# Shared cache DB — used by backend (read) and collector (read/write)
CACHE_DATASOURCE_URL=jdbc:postgresql://db:${DB_PORT}/${DB_NAME_CACHE}
CACHE_DATASOURCE_USERNAME=${DB_USERNAME}
CACHE_DATASOURCE_PASSWORD=${DB_PASSWORD}


############################################################
# === APP ADMIN SETTINGS (unchanged) ===
############################################################

APP_ADMIN_EMAIL=mrks.dunkel@gmail.com
APP_ADMIN_PASSWORD=placeholder
DEMO_SECRET=placeholder


############################################################
# === TRAEFIK / ROUTING (extended for staging) ===
############################################################

# Production host
APP_HOST=app.homewatts.xyz

# staging host
APP_HOST_STAGING=staging.homewatts.xyz

# Let's Encrypt email for Traefik
ACME_EMAIL=mrks.dunkel@gmail.com


############################################################
# === IMAGE TAGS (NEW - used by GitHub Actions deploy) ===
############################################################

# Default values; CI/CD overrides these during deployment
IMAGE_TAG_PROD=latest
IMAGE_TAG_STAGING=latest
IMAGE_TAG_COLLECTOR=latest
